version: 2
updates:
  # Keeps the SHA-pinned actions current: one grouped PR a week, reviewed like any other.
  - package-ecosystem: github-actions
    directory: /
    schedule:
      interval: weekly
    groups:
      actions:
        patterns: ["*"]
